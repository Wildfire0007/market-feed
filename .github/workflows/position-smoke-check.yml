name: position-smoke-check

on:
  pull_request:
  push:
    branches: ["main", "master"]

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run position tracking smoke check
        run: python tools/smoke_check_positions.py
